# User Authentication & Profile Management System

## 📌 Overview

This is a simple user authentication system built using the MEN stack (MongoDB, Express.js, Node.js) with a basic HTML/CSS/JavaScript frontend. The application allows users to register, login, and access a protected profile page.

## 🚀 Features

- User Registration
- User Login
- JWT Authentication
- Password Hashing with bcrypt
- Protected Routes
- User Profile Page
- MongoDB Database Integration

## 🛠️ Technologies Used

- Node.js
- Express.js
- MongoDB (Atlas)
- Mongoose
- JWT
- HTML, CSS, JavaScript

## 🔐 Environment Variables

Create a `.env` file in the root directory with:
MONGO_URI=your_mongodb_uri
JWT_SECRET=your_secret

## ⚙️ Installation

1. Clone the repository

```bash
git clone https://github.com/Melcodes21/men-auth-system.git
```
